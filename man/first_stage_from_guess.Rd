% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/first-stage.R
\name{first_stage_from_guess}
\alias{first_stage_from_guess}
\title{Run the First Stage of an Algorithm from an Initial Guess}
\usage{
first_stage_from_guess(
  transition_model_guess,
  data_for_est,
  opts = list(),
  unit_weights = NULL,
  max_iter_override = NULL,
  compute_empirical_Ps = TRUE
)
}
\arguments{
\item{transition_model_guess}{An initial guess for the transition model.}

\item{data_for_est}{A list returned by \code{split_data_for_estimation()}, containing
state sequences and indicators split by treatment status.}

\item{opts}{(Optional) A list of options for the algorithm, including
maximum iterations and tolerance level.}

\item{unit_weights}{A numeric vector of weights for each unit, or \code{NULL}.
If \code{NULL}, equal weights are assumed.}

\item{max_iter_override}{(Optional) If provided, overrides the max_iter from opts.
Used internally by two-stage multistart to control short/long run iterations.}

\item{compute_empirical_Ps}{Logical; if TRUE (default), computes empirical transition
matrices from pre-treatment periods. Set to FALSE during short run to save computation.}
}
\value{
The transition model after running the first stage of the algorithm.
}
\description{
This function executes the first stage of an algorithm, starting from a
given initial transition model guess. It iterates through E and M steps until
convergence or the maximum number of iterations is reached.
}
