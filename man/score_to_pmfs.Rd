% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/likelihood-score-conversion.R
\name{score_to_pmfs}
\alias{score_to_pmfs}
\title{Convert List of Score Matrices to Probability Mass Functions (PMFs)}
\usage{
score_to_pmfs(pmfs_score, pmfs, weights = NULL, minimal = TRUE)
}
\arguments{
\item{pmfs_score}{A list of \eqn{J} elements, where each element is an \eqn{N \times (K-\mathrm{minimal})}
matrix of score values, with each row corresponding to an observation and each column
corresponding to a possible outcome category.}

\item{pmfs}{A list of \eqn{J} numeric vectors, each of length \eqn{K}, representing the
initial PMFs.}

\item{weights}{An optional numeric vector of length \eqn{N}, representing observation weights.
If provided, scores are weighted accordingly.}

\item{minimal}{A logical value. If TRUE, expects \eqn{N \times (K-1)} matrix
for scores.}
}
\value{
A list of \eqn{J} numeric vectors, each of length \eqn{K}, representing the updated PMFs.
}
\description{
This function converts a list of score matrices into a list of updated
probability mass functions (PMFs) by normalizing across observations.
}
\examples{
set.seed(123)
N <- 100
K <- 3
J <- 2
pmfs_score <- lapply(1:J, function(x) matrix(runif(N * K), nrow = N, ncol = K))
pmfs <- lapply(1:J, function(x) rep(1/K, K))  # Uniform initial PMFs
weights <- runif(N)

updated_pmfs <- score_to_pmfs(pmfs_score, pmfs, weights)
print(length(updated_pmfs))  # Should be J
print(length(updated_pmfs[[1]]))  # Should be K
}
