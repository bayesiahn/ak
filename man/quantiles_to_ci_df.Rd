% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-utils.R
\name{quantiles_to_ci_df}
\alias{quantiles_to_ci_df}
\title{Convert Quantiles to Confidence Interval Data Frame}
\usage{
quantiles_to_ci_df(
  quantiles,
  parameter_name_control,
  parameter_name_treated = NULL,
  current_index = 1,
  past_index = 1
)
}
\arguments{
\item{quantiles}{A list containing quantile estimates, such as bootstrap quantiles, along with group
information (\code{priors}, \code{treated_period}, \code{Ps_treated}, etc.).}

\item{parameter_name_control}{The name of the parameter for the control group.}

\item{parameter_name_treated}{(Optional) The name of the parameter for the treated group. Defaults to
\code{parameter_name_control} if not specified.}

\item{current_index}{The index corresponding to the current period in the matrices (default is 1).}

\item{past_index}{The index corresponding to the past period in the matrices (default is 1).}
}
\value{
A data frame with columns \code{j} (group index), \code{g} (treatment indicator), \code{t} (time period),
and \code{ci} (confidence interval values).
}
\description{
This function converts quantile estimates from a model into a data frame containing confidence intervals
(CIs) for treated and control groups across different time periods. It uses parameter values extracted
from the quantiles object to compute the confidence intervals.
}
\examples{
quantiles <- list(
  priors = list(1, 2),
  treated_period = 1,
  Ps_treated = list(matrix(0.1, 5, 5), matrix(0.2, 5, 5)),
  control_param = list(list(rep(0.5, 5))),
  treated_param = list(list(rep(0.6, 5)))
)
quantiles_to_ci_df(quantiles, "control_param", "treated_param")
}
\keyword{internal}
