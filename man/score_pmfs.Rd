% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/likelihood-score.R
\name{score_pmfs}
\alias{score_pmfs}
\title{Compute Score Function for Mixture Component PMFs}
\usage{
score_pmfs(y_indices, pmfs, posteriors, t_index = NULL, minimal = TRUE)
}
\arguments{
\item{y_indices}{Integer vector of length \eqn{N}, where each entry
represents the observed category index (values in \eqn{\{1, \ldots, K\}}).}

\item{pmfs}{A list of \eqn{J} numeric vectors, where each vector has length \eqn{K},
representing the initial probability mass function (PMF) for each mixture component.}

\item{posteriors}{A numeric \eqn{N \times J} matrix, where each entry represents
the posterior probability of observation \eqn{i} belonging to component \eqn{j}.}

\item{t_index}{An optional time index to append to the column names.}

\item{minimal}{A logical value. If TRUE, returns \eqn{N \times (K-1)} matrices.}
}
\value{
A list of \eqn{J} matrices, where each matrix has dimensions \eqn{N \times (K-\mathrm{minimal})},
representing the score contributions for each mixture component.
}
\description{
This function calculates the score function for the probability mass functions (PMFs)
of multiple mixture components, given observed outcome indices.
}
\examples{
y_indices <- c(1, 2, 1, 3)
pmfs <- list(c(0.2, 0.5, 0.3), c(0.3, 0.4, 0.3))
posteriors <- matrix(c(0.7, 0.8, 0.6, 0.9, 0.3, 0.2, 0.4, 0.1), nrow = 4, byrow = FALSE)

scores <- score_pmfs(y_indices, pmfs, posteriors)
print(dim(scores[[1]]))  # Should be (4, 3)

}
