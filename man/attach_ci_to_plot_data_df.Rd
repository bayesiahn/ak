% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-utils.R
\name{attach_ci_to_plot_data_df}
\alias{attach_ci_to_plot_data_df}
\title{Attach Confidence Intervals to Plot Data Frame}
\usage{
attach_ci_to_plot_data_df(
  plot_data_df,
  model,
  parameter_name_control,
  parameter_name_treated = NULL,
  current_index = 1,
  past_index = 1,
  uniform_ci = TRUE
)
}
\arguments{
\item{plot_data_df}{A data frame containing the plot data. Must have columns
\code{j}, \code{g}, \code{t}, and \code{y} (point estimates).}

\item{model}{A model object that contains \code{bootstrap_estimates} used for
calculating confidence intervals.}

\item{parameter_name_control}{Name of the parameter for the control group.}

\item{parameter_name_treated}{(Optional) Name of the parameter for the treated
group. Defaults to the value of \code{parameter_name_control}.}

\item{current_index}{The index corresponding to the current period (default is 1).}

\item{past_index}{The index corresponding to the past period (default is 1).}

\item{uniform_ci}{Logical; if TRUE (default), compute uniform (simultaneous)
confidence intervals; if FALSE, compute pointwise intervals.}
}
\value{
A data frame with confidence intervals (\code{ci_upper} and \code{ci_lower}) added.
The CIs are symmetric around the point estimate.
}
\description{
This function attaches confidence intervals (CIs) to a given plot data frame
based on a model's bootstrap estimates using the percentile-t (studentized
bootstrap) method.
}
\details{
The CIs are constructed by:
\enumerate{
\item Computing bootstrap standard deviations for each (j, g, t) combination.
\item Computing bootstrap t-statistics: (boot_estimate - estimate) / bootstrap_sd.
\item For uniform CIs: using the max |t| across all periods as the critical value.
\item For pointwise CIs: using the 95th percentile of |t| at each period.
\item Returning symmetric CIs: estimate +/- critical_value * sd.
}
}
\keyword{internal}
