% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-models.R
\name{plot_from_plot_data_df}
\alias{plot_from_plot_data_df}
\title{Plot Event Study Results from a Data Frame}
\usage{
plot_from_plot_data_df(
  plot_data_df,
  recenter_t = TRUE,
  display_vertical_event_lines = TRUE,
  display_lines = TRUE,
  y_lab = NULL,
  point_size = NULL
)
}
\arguments{
\item{plot_data_df}{A data frame containing the event study data. Must include columns \code{t} (time),
\code{y} (outcome), \code{g} (group), and \code{j} (cohort). Optionally, it can include columns \code{ci_lower} and \code{ci_upper} for confidence intervals.}

\item{recenter_t}{Logical. If \code{TRUE}, recenters the time variable \code{t} relative to the maximum value of \code{g}.}

\item{display_vertical_event_lines}{Logical. If \code{TRUE}, adds vertical dashed lines for event time points.}

\item{display_lines}{Logical. If \code{TRUE}, displays lines connecting points.}

\item{y_lab}{A character string specifying the label for the y-axis. Defaults to \code{NULL}, in which case a default label is applied.}

\item{point_size}{A numeric value specifying the size of the points. Defaults to \code{1}.}
}
\value{
A ggplot object representing the event study plot.
}
\description{
This function generates a plot from a data frame containing event study results.
The plot includes points, lines, and optional confidence intervals. It supports
customization of axis labels, vertical event lines, and recentering of time periods.
}
\details{
\itemize{
\item The function uses \code{ggplot2} for visualization and assumes the input data frame has been preprocessed for plotting.
\item If the \code{type} column is not already present in \code{plot_data_df}, it is created as the interaction of \code{g} and \code{j}.
\item Confidence intervals are displayed using \code{geom_errorbar} if the \code{display_ci} argument is \code{TRUE} and the relevant columns are available.
\item Vertical event lines are controlled using the \code{display_vertical_event_lines} argument and added at appropriate time points.
}
}
\examples{
# Example data frame
plot_data_df <- data.frame(
  t = c(-1, 0, 1, -1, 0, 1),
  y = c(0.2, 0.3, 0.4, 0.1, 0.15, 0.2),
  g = c(0, 0, 0, 1, 1, 1),
  j = c(1, 1, 1, 2, 2, 2),
  ci_lower = c(0.1, 0.2, 0.3, 0.05, 0.1, 0.15),
  ci_upper = c(0.3, 0.4, 0.5, 0.15, 0.2, 0.25)
)

# Generate the plot
plot_from_plot_data_df(
  plot_data_df,
  display_ci = TRUE,
  recenter_t = TRUE,
  display_vertical_event_lines = TRUE,
  y_lab = "Outcome"
)

}
