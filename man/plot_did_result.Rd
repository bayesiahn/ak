% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-event-study.R
\name{plot_did_result}
\alias{plot_did_result}
\title{Plot an Event-Study from a DID Result}
\usage{
plot_did_result(did_result, uniform_ci = FALSE)
}
\arguments{
\item{did_result}{An object returned by \code{did::att_gt()}, representing
group-time average treatment effect estimates.}

\item{uniform_ci}{Logical; if \code{TRUE}, plots uniform (simultaneous) confidence
intervals instead of pointwise ones. Passed to the \code{cband} argument in
\code{did::aggte()}. Defaults to \code{FALSE}.}
}
\value{
A \code{ggplot} object showing event-time dynamic ATT estimates with
confidence intervals, using:
\itemize{
\item integer x-axis labels (post-event periods start at 1),
\item no minor grid lines on the x-axis,
\item customized axis labels and no title,
\item legend placed at the bottom.
}
}
\description{
Produces a clean event-study plot from a \code{did} estimation object, using the
aggregated dynamic treatment effects obtained via \code{did::aggte()}.
The function adjusts the post-event period labels to start from 1 (rather than 0)
and formats the plot with a minimalist publication-style theme.
}
\details{
The function calls:
\enumerate{
\item \code{did::aggte()} with \code{type = "dynamic"} to compute event-study estimates;
\item \code{did::ggdid()} to generate the base event-study plot;
\item adjusts x-axis labels so that the post-event periods are labeled starting from 1;
\item applies consistent theming for clean presentation.
}
}
\examples{
\dontrun{
# Example using the simulated dataset from the did package
data(simdata)
attgt <- did::att_gt(yname = "Y",
                     tname = "year",
                     idname = "id",
                     gname = "G",
                     xformla = ~ X,
                     data = simdata)
plot_did_result(attgt, uniform_ci = TRUE)
}

}
