% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/likelihood-score-conversion.R
\name{score_to_P_jt}
\alias{score_to_P_jt}
\title{Convert Score List to Transition Probability Matrix}
\usage{
score_to_P_jt(P_jt_score, P_jt, weights = NULL, minimal = TRUE)
}
\arguments{
\item{P_jt_score}{A list of \eqn{K_t} elements, where each element is an
\eqn{N \times (K_{t-1}-\mathrm{minimal})} matrix of score values.}

\item{P_jt}{A \eqn{K_t \times K_{t-1}} matrix representing the initial transition probabilities.}

\item{weights}{An optional numeric vector of length \eqn{N}, representing
observation weights. If provided, scores are weighted accordingly.}

\item{minimal}{A logical value. If TRUE, expects \eqn{N \times (K_{t-1}-1)} matrix
for scores.}
}
\value{
A \eqn{K_t \times K_{t-1}} transition probability matrix.
}
\description{
This function converts a list of \eqn{N \times K_{t-1}} score matrices into
a \eqn{K_t \times K_{t-1}} transition probability matrix, adjusting it based on
an initial transition probability matrix.
}
\examples{
set.seed(123)
N <- 100
K_t <- 3
K_t_minus_1 <- 3
P_jt_score <- lapply(1:K_t, function(x) matrix(runif(N * K_t_minus_1), nrow = N, ncol = K_t_minus_1))
P_jt <- matrix(1/K_t_minus_1, K_t, K_t_minus_1)  # Uniform initial transitions
weights <- runif(N)

updated_P_jt <- score_to_P_jt(P_jt_score, P_jt, weights)
print(dim(updated_P_jt))  # Should be (K_t, K_{t-1})
}
