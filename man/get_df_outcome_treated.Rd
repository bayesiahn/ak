% Generated by roxygen2: do not edit by hand
% Please edit documentation in
%   R/empirical-examples-plots-counterfactuals-utils.R
\name{get_df_outcome_treated}
\alias{get_df_outcome_treated}
\title{Get Outcome Means for Treated Units by Period}
\usage{
get_df_outcome_treated(y_numeric, g, type_name_treated = "Treated")
}
\arguments{
\item{y_numeric}{A numeric vector representing the outcome variable.}

\item{g}{A vector indicating group membership (e.g., treatment cohort). Groups with \code{g == 0} are treated as controls and excluded.}

\item{type_name_treated}{A character string labeling the resulting outcome series; defaults to \code{"Treated"}.}
}
\value{
A data frame with the following columns:
\describe{
\item{t}{Time period.}
\item{y}{Weighted mean of the outcome variable for treated units.}
\item{type}{Label identifying the outcome series.}
}
}
\description{
Computes the weighted mean of a numeric outcome variable for treated groups (excluding control group \code{g == 0})
across time periods. It returns a data frame with time periods and corresponding outcome means.
}
\details{
This function uses uniform weights (i.e., all weights equal to 1) and excludes observations where \code{g == 0}
(typically representing untreated or never-treated units).
}
\seealso{
\code{\link[=get_weighted_means_by_gjt]{get_weighted_means_by_gjt()}}
}
