% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/likelihood-score-conversion.R
\name{score_to_pmf}
\alias{score_to_pmf}
\title{Convert Score Matrix to Probability Mass Function (PMF)}
\usage{
score_to_pmf(pmf_j_score, pmf_j, weights = NULL, minimal = TRUE)
}
\arguments{
\item{pmf_j_score}{A numeric \eqn{N \times (K-\mathrm{minimal})} matrix of score values, where
each row corresponds to an observation and each column corresponds to
a possible outcome category.}

\item{pmf_j}{A numeric vector of length \eqn{K}, representing the initial PMF.}

\item{weights}{An optional numeric vector of length \eqn{N}, representing
observation weights. If provided, scores are weighted accordingly.}

\item{minimal}{A logical value. If TRUE, expects \eqn{N \times (K-1)} matrix
for scores.}
}
\value{
A numeric vector of length \eqn{K}, representing the updated PMF.
}
\description{
This function converts an \eqn{N \times (K-\mathrm{minimal})} matrix of score values into
a probability mass function (PMF) by normalizing across observations,
adjusting it based on an initial PMF.
}
\examples{
set.seed(123)
N <- 100
K <- 3
pmf_j_score <- matrix(runif(N * K), nrow = N, ncol = K)
pmf_j <- rep(1/K, K)  # Uniform initial PMF
weights <- runif(N)

pmf <- score_to_pmf(pmf_j_score, pmf_j, weights)
print(sum(pmf))  # Should be a normalized PMF
}
