% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-score.R
\name{scores_to_model}
\alias{scores_to_model}
\title{Convert Score List to Transition Model}
\usage{
scores_to_model(scores, weights = NULL)
}
\arguments{
\item{scores}{A list containing score matrices for priors, transition probabilities,
and probability mass functions. The expected structure of \code{scores} includes:
\itemize{
\item \code{g}: Treatment assignment vector.
\item \code{priors}: A numeric score vector for prior probabilities.
\item \code{priors_treated}: A numeric score vector for prior probabilities in the treated group.
\item \code{Ps_control}: A list of transition score matrices for the control group.
\item \code{Ps_treated}: A list of transition score matrices for the treated group.
\item \code{Ps_control_empirical}: A list of empirical transition score matrices for the control group.
\item \code{Ps_treated_empirical}: A list of empirical transition score matrices for the treated group.
\item \code{pmfs_initial_control}: A list of score matrices for initial PMFs in the control group.
\item \code{pmfs_initial_treated}: A list of score matrices for initial PMFs in the treated group.
\item \code{pmfs_pre_treatment_control}: A list of score matrices for pre-treatment PMFs in the control group.
\item \code{pmfs_pre_treatment_treated}: A list of score matrices for pre-treatment PMFs in the treated group.
\item \code{treated_period}: The period when treatment starts.
\item \code{Y}: A list containing outcome names.
}}

\item{weights}{An optional numeric vector of weights for each observation, which is applied
when computing priors, transition probabilities, and PMFs.}
}
\value{
A list representing a structured transition model with the following elements:
\itemize{
\item \code{priors}: Estimated prior probabilities.
\item \code{priors_treated}: Estimated prior probabilities for the treated group.
\item \code{prob_treated}: Estimated probability of treatment.
\item \code{Ps_control}: Estimated transition probabilities for the control group.
\item \code{Ps_treated}: Estimated transition probabilities for the treated group.
\item \code{Ps_control_empirical}: Estimated empirical transition probabilities for the control group.
\item \code{Ps_treated_empirical}: Estimated empirical transition probabilities for the treated group.
\item \code{pmfs_initial_control}: Estimated initial PMFs for the control group.
\item \code{pmfs_initial_treated}: Estimated initial PMFs for the treated group.
\item \code{pmfs_pre_treatment_control}: Estimated pre-treatment PMFs for the control group.
\item \code{pmfs_pre_treatment_treated}: Estimated pre-treatment PMFs for the treated group.
\item \code{treated_period}: The time period when treatment starts.
\item \code{Y}: The outcome names.
The returned object is of class \code{"TransitionModel"}.
}
}
\description{
This function converts a list of score functions into a structured transition model,
which includes estimated priors, transition probabilities, and probability mass functions (PMFs).
}
\examples{
set.seed(123)
scores <- list(
  priors = runif(3),
  priors_treated = runif(3),
  Ps_control = list(matrix(runif(9), ncol = 3), matrix(runif(9), ncol = 3)),
  Ps_treated = list(matrix(runif(9), ncol = 3), matrix(runif(9), ncol = 3)),
  Ps_control_empirical = list(matrix(runif(9), ncol = 3), matrix(runif(9), ncol = 3)),
  Ps_treated_empirical = list(matrix(runif(9), ncol = 3), matrix(runif(9), ncol = 3)),
  pmfs_initial_control = list(matrix(runif(9), ncol = 3)),
  pmfs_initial_treated = list(matrix(runif(9), ncol = 3)),
  pmfs_pre_treatment_control = list(matrix(runif(9), ncol = 3)),
  pmfs_pre_treatment_treated = list(matrix(runif(9), ncol = 3)),
  treated_period = 2,
  Y = list(names = c("Low", "Medium", "High"))
)
model <- scores_to_model(scores)
print(class(model))  # Should be "TransitionModel"

}
