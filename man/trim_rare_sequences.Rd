% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/empirical-examples-models-did-conditional-lag.R
\name{trim_rare_sequences}
\alias{trim_rare_sequences}
\title{Trim rare past outcome sequences by control group prevalence}
\usage{
trim_rare_sequences(did_df, n_trim_threshold = 5)
}
\arguments{
\item{did_df}{A data frame that includes at least the columns \code{treatment_cohort},
\code{past_outcome_sequence}, and \code{t}.}

\item{n_trim_threshold}{An integer. A sequence will be trimmed if it occurs fewer than
this number of times in the control group but is present in the treated group.}
}
\value{
A list with two elements:
\describe{
\item{did_df_trimmed}{The data frame after removing rows with rare sequences.}
\item{summary}{A list with trimming metadata, including:}
\describe{
\item{past_outcome_sequence_table}{A wide-format summary table showing sequence counts
for treated and control groups at baseline.}
\item{trimmed_sequences}{A character vector of sequences that were trimmed.}
\item{n_trim_threshold}{The threshold used to define rarity.}
}
}
}
\description{
This function trims rows from a DID-style dataframe where a \code{past_outcome_sequence} is
uncommon in the control group (i.e., occurs fewer than \code{n_trim_threshold} times at baseline
period, defined as \code{t == 1}), but is present in the treated group. This helps ensure that
each treated sequence has sufficient support in the control group for valid comparisons.
}
