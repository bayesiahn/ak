% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/first-stage-M-step.R
\name{M_step_initial_outcome_dists}
\alias{M_step_initial_outcome_dists}
\title{Compute Initial Distributions for Each Latent Group}
\usage{
M_step_initial_outcome_dists(
  posteriors,
  initial_outcome_indicators,
  unit_weights = NULL
)
}
\arguments{
\item{posteriors}{A matrix of posterior probabilities for each individual
belonging to each group.}

\item{initial_outcome_indicators}{A list of one-hot encoded vectors representing
the initial state of each individual.}

\item{unit_weights}{A numeric vector of weights for each unit, or \code{NULL}.
If \code{NULL}, equal weights are assumed.}
}
\value{
A list of initial distributions, one for each latent group.
}
\description{
This function calculates the initial distributions for each latent group
based on posteriors and initial outcome indicators. The function applies
\code{M_step_initial_outcome_dists_j} across all latent groups, optionally using
unit weights for weighted calculations.
}
\examples{
posteriors <- matrix(runif(6), nrow = 2)
initial_outcome_indicators <- rbind(c(1, 0), c(0, 1))
initial_dists <- M_step_initial_outcome_dists(posteriors, initial_outcome_indicators)
print(initial_dists)
}
